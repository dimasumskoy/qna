$('.answer_list').html("<%= j render 'questions/answer_list' %>");
$('#answer_body').val("");

<% if @answer.errors.any? %>
  <% @answer.errors.full_messages.each do |message| %>
    $('.answer_list').append('<%= j message %>');
  <% end %>
<% end %>

